{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)",
      "Bash(git branch:*)",
      "Bash(.venv/bin/python -m pytest:*)",
      "Bash(.venv/bin/ruff:*)",
      "Bash(gh pr view:*)",
      "Bash(gh pr checks:*)",
      "Bash(gh run list:*)",
      "Bash(gh run view:*)",
      "Bash(gh issue view:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,subprocess,sys,pathlib; p=json.load(sys.stdin).get('tool_input',{}).get('file_path',''); sys.exit(0) if not p.endswith('.py') else None; ruff=pathlib.Path('.venv/bin/ruff'); subprocess.run([str(ruff) if ruff.exists() else 'ruff','format','--quiet',p],check=False)\""
          }
        ]
      }
    ]
  }
}
